  <small class="" ><%= "#{@post.title} by #{@user.name}" %></small>
  <small class="">Comments: <%= @post.comments_counter%></small>
  <small>Likes: <%= @post.likes_counter%></small>
    <p class=""><%= @post.text%></p>
</div>
<div class="">
    <% @post.comments.each do |comment| %>
    <div class="">
        <small><%= comment.user.name%>: </small>
        <small><%= comment.text%> </small>
    </div>
    <% end %>
</div>
